{
  "name": "typescript-banking-2026",
  "version": "1.0.0",
  "private": true,
  "description": "Strict TypeScript banking domain modeling and business-rule examples for React/BFSI applications.",
  "type": "module",
  "scripts": {
    "build": "npm run typecheck",
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
